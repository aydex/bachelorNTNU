describe('Transactions person', function() {
    'use strict';

    it('retrieves all transactions on a given person', function () {
        var test = true;
        expect(test).toBeTruthy();
    });
    
    it('displays informational tooltips on hover', function() {
        var test = true;
        expect(test).toBeTruthy();
    });
    
    it('redirects when a transaction is clicked', function() {
        var test = true;
        expect(test).toBeTruthy();
    })
});