describe('SVG Map', function() {
    'use strict';

    it ('generates an interactive country map', function() {
        var test = true;
        expect(test).toBeTruthy();
    });

    it ('makes each county clickable by id', function() {
        var test = true;
        expect(test).toBeTruthy();
    });

    describe('Region', function() {
        it ('generalizes the counties into regions', function() {
            var test = true;
            expect(test).toBeTruthy();
        });

        it ('makes each municipality clickable by id', function() {
            var test = true;
            expect(test).toBeTruthy();
        });

        it ('displays a tooltip on hover', function() {
            var test = true;
            expect(test).toBeTruthy();
        })
    });

    describe('City', function() {
        it ('redirects to the proper search on click', function() {
            var test = true;
            expect(test).toBeTruthy();
        });

        it ('displays a tooltip on hover', function() {
            var test = true;
            expect(test).toBeTruthy();
        })
    })
});