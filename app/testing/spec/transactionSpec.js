describe('Transaction service', function() {
    'use strict';

    it('sets last name after first name', function () {
        var test = true;
        expect(test).toBeTruthy();
    });

    it('capitalizes the first letter of a given word', function() {
        var test = true;
        expect(test).toBeTruthy();
    });

    it('abbreviates middle names', function() {
        var test = true;
        expect(test).toBeTruthy();
    });

    it('returns K when receving a municipality', function() {
        var test = true;
        expect(test).toBeTruthy();
    });

    it('returns the role of a party in a transaction', function() {
        var test = true;
        expect(test).toBeTruthy();
    })
});