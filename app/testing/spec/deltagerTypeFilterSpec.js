describe('Deltager type filter', function() {
    'use strict';

    it ('turns the short hand for a participant into the full word', function() {
        var test = true;
        expect(test).toBeTruthy();
    });
});