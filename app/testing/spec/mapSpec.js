describe('Map controller', function() {
    'use strict';

    it ('makes the tooltip move to the correct position', function() {
        var test = true;
        expect(test).toBeTruthy();
    });
});