describe('Transactions property timeline', function() {
    'use strict';

    it('generates a timeline with transactions', function () {
        var test = true;
        expect(test).toBeTruthy();
    });
});