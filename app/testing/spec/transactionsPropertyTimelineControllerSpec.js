describe('Transactions property timeline controller', function() {
    'use strict';

    it('populates the timeline with transactions', function () {
        var test = true;
        expect(test).toBeTruthy();
    });
});