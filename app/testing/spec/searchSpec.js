describe('Search controller', function() {
   it ('does a search', function() {
       var test = true;
       expect(test).toBeTruthy();
   });

    it ('does a name search', function() {
        var test = true;
        expect(test).toBeTruthy();
    });

    it ('does an address search', function() {
        var test = true;
        expect(test).toBeTruthy();
    });
    
    it ('gets county by number', function() {
        var test = true;
        expect(test).toBeTruthy();
    });
    
    it ('gets city by number', function() {
        var test = true;
        expect(test).toBeTruthy();
    });

    it ('shows transactions involving a person', function() {
        var test = true;
        expect(test).toBeTruthy();
    });

    it ('shows transactions involving a city', function() {
        var test = true;
        expect(test).toBeTruthy();
    });

    it ('sets search text', function() {
        var test = true;
        expect(test).toBeTruthy();
    });
});